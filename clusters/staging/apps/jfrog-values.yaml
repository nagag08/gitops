apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: artifactory
spec:
  interval: 15m
  chart:
    spec:
      chart: postgresql
      sourceRef:
        kind: HelmRepository
        name: jfrog
      interval: 12h
      version: "16.7.26"
  test:
    enable: false
  valuesFrom:
    - kind: ConfigMap
      name: jfrog-values
      valuesKey: values.yaml
