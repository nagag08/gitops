apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: artifactory
spec:
  chart:
    spec:
      chart: artifactory
      sourceRef:
        kind: HelmRepository
        name: jfrog
      interval: 12h
      version: "107.x"
  test:
    enable: false
  valuesFrom:
    - kind: ConfigMap
      name: jfrog-values
      valuesKey: values.yaml
